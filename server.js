const express = require('express');
const app = express();
const port = 3000;

// Ваш код для маршрутов и обработки запросов здесь...

app.listen(port, () => {
    console.log(`Сервер запущен на порте ${port}`);
});